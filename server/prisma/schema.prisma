// Prisma schema for UNI-NEST.
// Full data models will be added in Phase 2 (Database Design).

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}
