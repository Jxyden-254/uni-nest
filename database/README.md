# Database

This folder holds database design artifacts for UNI-NEST:

- ER diagrams (added in Phase 2)
- SQL scripts and seed data notes
- Database documentation

The actual Prisma schema and migrations live in `server/prisma/`.
