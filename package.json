{
  "name": "uni-nest",
  "version": "0.1.0",
  "private": true,
  "description": "UNI-NEST - Student accommodation platform (final-year project)",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.5.3"
  }
}
